# Jenkins — CI/CD local

Pipeline squelette (étape 2). Implémentation complète → **étape 9**.

## Prérequis

- Jenkins installé localement (Docker ou install natif Windows)
- Plugin Pipeline
- Docker disponible pour l'agent Jenkins

## Fichier

`jenkins/Jenkinsfile` — stages : checkout, test, build Docker.

**Responsable** : Berdan
